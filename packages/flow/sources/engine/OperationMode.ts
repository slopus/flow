export type OperationMode = {
    slug: string;
    name: string;
    description: string;
    icon: string;
    color: string;
    bypassPermissions: boolean;
    allowEdits: boolean;
    allowReadOnly: boolean;
}
